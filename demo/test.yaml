---
#List of Rules the Rule Engine/Converter will read:
  rules:
    - rule-name: "Employees aren't allowed to access the database from 11PM onwards!"
      event: "db_query"
      severity: "high" #Chosen from Critical, High, Med, Low
      actions:
        - type: "alert"
          params:
            target: "slack"
            channel: "#shits-on-fire"

  